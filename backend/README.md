# Backend API

Backend server for Set Over Set Website.

## Getting Started

```bash
npm install
npm start
```

The server will run on port 5001 by default (or the port specified in the PORT environment variable).

